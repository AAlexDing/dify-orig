identity:
  name: comfyworkflow
  author: Dinggg!
  label:
    en_US: ComfyUI Workflow
    zh_Hans: ComfyUI 工作流
    pt_BR: ComfyUI Workflow
description:
  human:
    en_US: Input an API format json string exported from ComfyUI, and generate an image. (You need to enable the developer options in ComfyUI and click "Export(API Format)")
    zh_Hans: 输入由ComfyUI导出的API格式的json文件，即可生成图像（注意：需要先在ComfyUI中启用开发者选项，并点击“Export(API Format)”）
    pt_BR: Input an API format json string exported from ComfyUI, and generate an image. (You need to enable the developer options in ComfyUI and click "Export(API Format)")
  llm: Generate an image using ComfyUI based on the given json file.
parameters:
  - name: json_string
    type: string
    required: true
    label:
      en_US: Json String
      zh_Hans: Json 字符串
      pt_BR: Json String
    human_description:
      en_US: An API format json string exported from ComfyUI(You need to enable the developer options in ComfyUI and click "Export(API Format)")
      zh_Hans: 由ComfyUI导出的API格式的json字符串（注意：需要先在ComfyUI中启用开发者选项，并点击“Export(API Format)”）
      pt_BR: An API format json string exported from ComfyUI(You need to enable the developer options in ComfyUI and click "Export(API Format)")
    llm_description: The API format json string exported from ComfyUI Workflow
    form: llm