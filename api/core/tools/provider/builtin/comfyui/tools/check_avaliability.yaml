identity:
  name: comfyui_check_avaliability
  author: Dinggg!
  label:
    en_US: Check Avaliability
    zh_Hans: 检查ComfyUI可用性
    pt_BR: Check Avaliability
description:
  human:
    en_US: Check if ComfyUI is available,if online return 1,if offline return 0
    zh_Hans: 检查ComfyUI是否可用，在线返回 1，不在线返回 0
    pt_BR: Check if ComfyUI is available,if online return 1,if offline return 0
  llm: Check if ComfyUI is available,if online return 1,if offline return 0